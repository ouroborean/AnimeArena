extends Node

var threat_assist = true
# When false, all per-action animations are suppressed and pacing pauses are
# skipped entirely — turns resolve as fast as the underlying logic allows.
# Toggled from the in-game settings menu.
var animations_enabled = true
